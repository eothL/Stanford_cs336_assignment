# Stanford

`Stanford/` is a big project grouping Stanford coursework and related subprojects. Each subfolder (e.g. `assignment_1/`, `assignment2-systems/`, `basic/`) is a **subproject** with its own `CLAUDE.md` for specific context.

Also read the parent `Learning_hub/CLAUDE.md` for the canonical operating rules.
